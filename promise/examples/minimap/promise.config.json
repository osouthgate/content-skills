{
  "version": 1,
  "$comment": "The config for this example project. It sits at the fallback location (<project>/promise.config.json) so it is visible; a real project puts it at .claude/promise.config.json. Every map command is an argv line run with no shell from this directory, so `python3 capability_find.py` resolves against the script beside this file.",
  "docsHome": "docs/designs",
  "map": {
    "recipe": "recipe.md",
    "find": "python3 capability_find.py",
    "row": "python3 capability_find.py --row",
    "nextId": "python3 capability_find.py --next-id",
    "rowIdPattern": "^CAP-\\d+$",
    "lanes": {
      "data": "tests[]",
      "response": "tests[]",
      "perception": "e2eTests[]",
      "judgement": "evalTests[]",
      "sibling": "siblingTests[]"
    },
    "checks": [
      "python3 capability_find.py --check"
    ]
  }
}
