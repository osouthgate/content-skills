{
  "name": "content-skills",
  "owner": {
    "name": "Oliver Southgate",
    "url": "https://github.com/osouthgate"
  },
  "metadata": {
    "description": "Content and workflow plugins for Claude Code: YouTube and podcast transcript extraction and summarization (youtube-transcript, podcast-transcript), autonomous planning and unattended execution (supergoal, autonomous-run), and outcome docs carried from intent to proof (promise).",
    "version": "0.3.0"
  },
  "plugins": [
    {
      "name": "youtube-transcript",
      "source": "./youtube-transcript",
      "description": "Extract YouTube captions/transcripts, transcribe local audio/video files, summarize transcript files, and fall back to OpenAI audio transcription when captions are unavailable."
    },
    {
      "name": "podcast-transcript",
      "source": "./podcast-transcript",
      "description": "Extract podcast transcripts from Spotify episode links, Apple Podcasts episode links, public RSS feeds, direct transcript/audio URLs, and local media; save clean Markdown and metadata, with optional summaries."
    },
    {
      "name": "supergoal",
      "source": "./supergoal",
      "description": "Plan and autonomously build a software task end-to-end under a single /goal, with retry, fix-spec recovery, per-phase memory writeback, and a final audit. Cross-platform (PowerShell + POSIX helpers). Adapted for Windows from supergoal by Robert Courson."
    },
    {
      "name": "autonomous-run",
      "source": "./autonomous-run",
      "description": "Configure Claude Code for a long unattended run: auto permissions, dynamic-workflow subagent fan-out, /goal or /loop persistence, cloud execution, and end-to-end self-verification. Detects which levers are available and hands off cleanly. Pairs with supergoal."
    },
    {
      "name": "promise",
      "source": "./promise",
      "description": "Write the promise before the build, then prove it kept. An outcome doc records the human's outcome line, rules and success signal verbatim in a protected §0 TLDR; the agent derives invariants, mechanism with file:line evidence, Given/When/Then acceptance rows (each tagged with the altitude its Then asserts on) and build phases; a linter checks the shape; `arm` commits the rows as failing tests before anything is built and records the commit in the doc; `reconcile` updates the doc when work lands. Where a project also keeps a machine-checked capability map, once a human marks the doc `agreed`, `arm` files the rows into it, `intake` routes feedback onto existing promises instead of new documents, and `reconcile` moves a row's verdict only when the evidence reaches the altitude its own Then claims. Eight modes behind one thin router, mode files loaded on demand, Python scripts (stdlib only) for orientation, shape linting and row extraction, and a per-project config instead of a fork. The outcome plugin was removed in promise 1.2.0; its framework lives on here."
    }
  ]
}
